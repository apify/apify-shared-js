import actorSchema from '../schemas/actor.schema.json' with { type: 'json' };
import datasetSchema from '../schemas/dataset.schema.json' with { type: 'json' };
import inputSchema from '../schemas/input.schema.json' with { type: 'json' };
import keyValueStoreSchema from '../schemas/key_value_store.schema.json' with { type: 'json' };
import outputSchema from '../schemas/output.schema.json' with { type: 'json' };

export { actorSchema, datasetSchema, inputSchema, keyValueStoreSchema, outputSchema };
